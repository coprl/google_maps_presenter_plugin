{
  "name": "google_maps_presenter_plugin",
  "version": "1.0.2",
  "description": "Google Maps plugin for COPRL",
  "main": "index.js",
  "scripts": {
    "watch": "esbuild index.js --bundle --watch --target=es2015 --outfile=../../public/bundle.js",
    "build": "esbuild index.js --bundle --minify --target=es2015 --outfile=../../public/bundle.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coprl/google_maps_presenter_plugin.git"
  },
  "author": "Evvnt <dev@evvnt.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/coprl/google_maps_presenter_plugin/issues"
  },
  "homepage": "https://github.com/coprl/google_maps_presenter_plugin#readme",
  "devDependencies": {
    "esbuild": "^0.18.17"
  }
}
